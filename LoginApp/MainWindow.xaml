<Window x:Class="LoginApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LoginApp"
        xmlns:uc="clr-namespace:UserClass;assembly=GameManagementClassLibrary"
        mc:Ignorable="d"
        Title="LoginWindow" Height="140" Width="400" Background="DeepSkyBlue">

    <Window.DataContext>
        <uc:UserManager/>
    </Window.DataContext>

    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="5*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0">
                <Label Content="USERNAME" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15"/>
            </Grid>

            <Grid Grid.Column="1">
                <Label Content="PASSWORD" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="GridUsername" Grid.Column ="0">
                <TextBox Text="{Binding User.Username, UpdateSourceTrigger=PropertyChanged}" Width="190" Height="30" BorderBrush="DeepSkyBlue" BorderThickness="3" TextChanged="TextBox_TextChanged"/>
            </Grid>

            <Grid x:Name="GridPassword" Grid.Column ="1">
                <TextBox x:Name="Password" Text="{Binding User.Password, UpdateSourceTrigger=PropertyChanged}" Width="190" Height="30" BorderBrush="DeepSkyBlue" BorderThickness="3" />
                <PasswordBox x:Name="PasswordBoxPassword" Width="190" Height="30" BorderBrush="DeepSkyBlue" BorderThickness="3"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="5*"/>
                <ColumnDefinition Width="5*"/>
            </Grid.ColumnDefinitions>

            <Grid x:Name="GridLOGIN" Grid.Column ="0">
                <Button x:Name="LOGIN" Content="LOGIN" Width="150" Height="25" Click="LOGIN_Click" Background="Azure" BorderBrush="DeepSkyBlue"/>
            </Grid>

            <Grid x:Name="GridLOGOUT" Grid.Column ="3">
                <Button x:Name="LOGOUT" Content="LOGOUT" Width="150" Height="25" Click="LOGOUT_Click" Background="Azure" BorderBrush="DeepSkyBlue"/>
            </Grid>
        </Grid>

        <Grid Grid.Row="3">
            <TextBox x:Name="InformationsMessage" Text="{Binding Information, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" BorderBrush="DeepSkyBlue" BorderThickness="3" IsReadOnly="True"/>
        </Grid>
    </Grid>
</Window>
